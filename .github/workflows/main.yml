name: Deploy to EKS

on:
  push:
    branches: [ "main" ]

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: my-fastapi-repo    # 👈 AWS ECR에 만든 이름과 똑같아야 함!
  EKS_CLUSTER: my-eks-cluster       # 👈 AWS EKS 클러스터 '진짜' 이름!

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build and Push image to ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.EKS_CLUSTER }}

      - name: Deploy to EKS
        run: |
          # 파일이 최상위에 있으므로 k8s/ 경로를 모두 제거함
          sed -i "s|CONTAINER_IMAGE|${{ steps.build-image.outputs.image }}|g" deployment.yaml
          kubectl apply -f deployment.yaml

      - name: Deploy to EKS
        run: |
          # 현재 어떤 파일들이 있는지 로그에 출력 (에러 찾기용)
          echo "Checking files in current directory:"
          ls -R

          # 만약 파일명이 deployment.yml (a가 빠진 것)이라면 아래 파일명도 수정해야 해!
          if [ -f "deployment.yaml" ]; then
            sed -i "s|CONTAINER_IMAGE|${{ steps.build-image.outputs.image }}|g" deployment.yaml
            kubectl apply -f deployment.yaml
          elif [ -f "deployment.yml" ]; then
            sed -i "s|CONTAINER_IMAGE|${{ steps.build-image.outputs.image }}|g" deployment.yml
            kubectl apply -f deployment.yml
          else
            echo "❌ Error: deployment.yaml or deployment.yml not found!"
            exit 1
          fi
